# Relaywright 1.1.0 Release Record

Status: release candidate preparation.

Date: 2026-07-25

## Candidate

- Target version: `1.1.0`
- Release branch: `release/1.1.0`
- Baseline stable release: `v1.0.2`
- Baseline stable commit: `ce2bc694e0176f51c514ce26005c1f2e94e3b2ae`
- Planned release candidate tag: `v1.1.0-rc.1`
- Release candidate commit: pending
- Release URL: pending
- Release workflow: pending

## Included Changes

- Adds a dashboard production-readiness checklist derived from existing configuration and recorded operational evidence.
- Links operators directly to HTTPS, upstream relay, trusted-device, submission-policy, diagnostics, backup, and alert settings.
- Treats successful connectivity and test-email diagnostics as current only when they were recorded after the latest relay configuration save.
- Refreshes the public README and GitHub Pages site with real sanitized screenshots and clearer installation, safety, support, and contribution entry points.
- Adds structured issue forms, a pull-request template, contribution guidance, and safe support-reporting guidance.
- Keeps Pages version validation synchronized with `Directory.Build.props`.

## Safety Scope

- SMTP DATA durability behavior is unchanged.
- Trusted-network checks and submission-policy enforcement are unchanged.
- Queue state transitions, spool cleanup ordering, delivery authentication, and secret protection are unchanged.
- The readiness checklist derives state from existing configuration and operational records; it does not create a parallel setup state.

## Local Prep Evidence

Release branch prep passed locally on 2026-07-25:

- `git diff --check -- .`: passed.
- `dotnet restore Relaywright.sln`: passed.
- Debug build: passed with 0 warnings and 0 errors.
- Debug tests: passed, 224/224.
- Release build: passed with 0 warnings and 0 errors.
- Release tests: passed, 224/224.
- `dotnet list Relaywright.sln package --vulnerable --include-transitive`: no vulnerable packages reported.
- `dotnet format Relaywright.sln --verify-no-changes --no-restore`: passed.
- PowerShell parser checks for the Windows installer engine, Windows release validator, and Pages validator: passed.
- Bash syntax checks for the Linux installer and release/ugly-path/soak validators: passed.
- `scripts/Validate-PagesSite.ps1`: passed with all five product screenshots validated as 1265 x 712 PNG images.
- Browser review: passed for the dashboard, sanitized product screenshots, v1.1.0 landing-page metadata, release links, and image gallery.

Local limitation:

- Inno Setup compilation is performed by the Release workflow on `windows-latest`; it is not part of this workstation's local gate.

## Release Candidate Evidence

Required candidate gates:

- Release workflow and artifact publication: pending.
- Windows `full-release` validation from `1.0.2`: pending.
- Linux x64 `full-release` validation from `1.0.2`: pending.
- Linux ARM64 clean-install validation: pending.
- Linux ugly-path validation: pending.
- Trusted/untrusted SMTP smoke: pending.
- Package vulnerability gates: pending.
- Website and Wiki release review: pending.

## Stable Release

Pending successful candidate validation and promotion through `main`.

## Stable Smoke

Pending.

## Website Publish

Pending.
