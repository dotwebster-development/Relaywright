# Relaywright 1.0.0-rc.6 Release Record

Status: failed, superseded by `v1.0.0-rc.7`.

Date: 2026-06-29

## Candidate

- Tag: `v1.0.0-rc.6`
- Commit: `c981fff11d5223dad9ea0ef5aa325dec99bea1f4`
- Artifact version: `1.0.0-rc.6`
- Release URL: https://github.com/dotwebster-development/Relaywright/releases/tag/v1.0.0-rc.6
- Release workflow: https://github.com/dotwebster-development/Relaywright/actions/runs/28395928592

## Assets

The Release workflow published the expected asset set:

- `Relaywright-1.0.0-rc.6-windows-x64-installer.exe`
- `relaywright-1.0.0-rc.6-windows-x64.zip`
- `relaywright-1.0.0-rc.6-linux-x64.tar.gz`
- `install-relaywright.sh`
- `SHA256SUMS.txt`

## Validation

- Branch protection for `main` and `development`: configured with required PRs, required `build-test`, strict/up-to-date checks, admin enforcement, and blocked force pushes/deletion.
- Windows full-release validation: passed on `DOT-WINDOWS02`.
  - Run: https://github.com/dotwebster-development/Relaywright/actions/runs/28396691656
- Linux full-release validation: passed on `dot-linux02`.
  - Run: https://github.com/dotwebster-development/Relaywright/actions/runs/28397064667
- SMTP smoke validation: failed.
  - Run: https://github.com/dotwebster-development/Relaywright/actions/runs/28397143632
  - Failure: trusted SMTP `RCPT TO:<recipient@example.test>` returned `550 mailbox unavailable`.
  - Root cause: Relaywright used `IMailbox.ToString()` for envelope addresses; the real `SmtpServer.Mail.Mailbox` implementation returned the type name instead of an address. Existing tests used a fake mailbox that overrode `ToString()`, masking the issue.

## Deviations And Follow-Up

- Machine names in this failed RC6 record are historical names from the run logs. Current workflow routing uses generic labels: `relaywright` plus `deploy`, `test`, or `soak`.
- The first Windows RC6 full-release attempt failed because `main` did not yet contain `scripts/windows/Install-Relaywright.ps1`; fixed by PR #1.
- The first Linux RC6 full-release attempts exposed install-VM/firewall readiness issues; `dot-linux02` was prepared with `ufw`, and Linux firewall detection was fixed by PR #2.
- RC6 must not be promoted to stable. The SMTP mailbox formatting fix landed in PR #3 and was released as `v1.0.0-rc.7`.

## Stable Tag

No stable `v1.0.0` tag was created from RC6.
