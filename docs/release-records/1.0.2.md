# Relaywright 1.0.2 Release Record

Status: preparing `v1.0.2-rc.1`.

Date: 2026-07-07

## Candidate

- Target version: `1.0.2`
- Release branch: `release/1.0.2`
- Baseline stable release: `v1.0.1`
- Baseline stable commit: `568e321ab8780f1416312c12e0848b83fa8c8d60`
- Release candidate tag: pending
- Release candidate commit: pending
- Release URL: pending
- Release workflow: pending

## Included Changes

- Windows installer overhaul:
  - Installer-managed install, update, uninstall, health verification, redacted diagnostics, and update rollback.
  - Structured SQLite, SQL Server, and MySQL database choices instead of raw connection-string entry.
  - Default/custom port flow with HTTPS, optional admin HTTP, and SMTP firewall port fields.
  - Focused firewall page with `LocalSubnet` default scope.
  - Final review page with redacted database and bootstrap settings.
  - Fixed service identity: service name `Relaywright`, display name `Relaywright - SMTP relay gateway`.
- Removed the installed standalone `Install-Relaywright.ps1` script from the Windows package.
- Updated Windows release validation to exercise installer-to-installer updates.
- Updated Windows installer docs, wiki drafts, release validation docs, and uninstall guidance.
- Shortened retention for large generated GitHub Actions handoff artifacts to keep release and deployment workflows from exhausting Actions storage.

## Actions Storage Prep

Actions storage cleanup was performed before starting the release branch:

- Deleted 121 generated package handoff artifacts from `dotwebster-development/Relaywright`.
- Removed only these artifact names: `relaywright-win-x64`, `relaywright-linux-x64`, `release-windows`, and `release-linux`.
- Freed about 7.58 GiB.
- Relaywright repo artifact storage after cleanup: about 1.21 GiB.

## Local Prep Evidence

Release branch prep passed locally on 2026-07-07:

- `git diff --check -- .`: passed.
- PowerShell parser checks for `installer/windows/RelaywrightInstallerEngine.ps1`, `scripts/windows/Invoke-RelaywrightWindowsReleaseValidation.ps1`, and `scripts/Validate-PagesSite.ps1`: passed.
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\Validate-PagesSite.ps1`: passed.
- `dotnet test tests\Relaywright.Web.Tests\Relaywright.Web.Tests.csproj`: passed, 217/217 tests.
- `dotnet build Relaywright.sln --configuration Release`: passed with 0 warnings and 0 errors.
- `dotnet test tests\Relaywright.Web.Tests\Relaywright.Web.Tests.csproj --configuration Release --no-build`: passed, 217/217 tests.
- `dotnet list Relaywright.sln package --vulnerable --include-transitive`: no vulnerable packages reported.

Local limitations:

- Inno Setup compile was not run locally because this workstation does not have `ISCC.exe` installed. The Release workflow installs Inno Setup before building the Windows installer.

## Release Candidate Evidence

Pending.

Required gates before stable promotion:

- Release workflow publishes `v1.0.2-rc.1` artifacts.
- Windows `full-release` validation passes against `1.0.2-rc.1` from `1.0.1`.
- Linux x64 `full-release` validation passes against `1.0.2-rc.1` from `1.0.1`.
- Linux ARM64 clean-install validation passes against `1.0.2-rc.1`.
- Linux ugly-path validation passes against `1.0.2-rc.1`.
- SMTP trusted/untrusted smoke passes against `1.0.2-rc.1`.
- Package vulnerability gates pass in the Release workflow and local prep evidence.

## Stable Release

Pending.

## Website Publish

Pending.
